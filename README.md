# Visa Global — Invitation Portal + Admin

## View design (no backend)
- Open `public/index.html` with VS Code Live Server.

## Full functionality (codes + admin + file uploads)
1) Install Node.js LTS
2) In this folder:
   npm install
   node server.js
3) Open:
   Client: http://localhost:3000/
   Admin:  http://localhost:3000/admin/login

## Admin credentials
Use env vars (recommended):
- ADMIN_USER
- ADMIN_PASS
- JWT_SECRET
